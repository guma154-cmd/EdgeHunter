# STORY-04-005: Parse robusto de JSON

## Metadata
- **Epic**: PRD-04 (GeminiValidator)
- **Status**: todo
- **Estimate**: 4h
- **Priority**: high
- **Assignee**: Rafael / AI Agent
- **Created**: 2026-05-15
- **Updated**: 2026-05-15

## User Story

**As a** sistema de backend  
**I want to** extrair dados estruturados das respostas de texto do Gemini  
**So that** eu possa tomar decisões lógicas baseadas na validação da IA.

## Description

LLMs nem sempre retornam JSON puro, mesmo quando solicitado. Esta story implementa um parser robusto (possivelmente usando `Pydantic` ou regex) para extrair os campos `is_valid` e `reason` de dentro da string de resposta, tratando markdown blocks (```json) e textos explicativos adicionais.

## Technical Details

### Files to Create/Modify
- `backend/app/utils/json_parser.py` (NEW)
- `tests/test_parser.py` (NEW)

### Code Structure
```python
# backend/app/utils/json_parser.py
def parse_gemini_json(text: str):
    # Procura por blocos json
    # Tenta json.loads
    # Faz fallback ou limpeza
    pass
```

### Dependencies (other stories)
- Depende de: STORY-04-002

## Acceptance Criteria

[ ] Extrai JSON mesmo se houver texto antes/depois.
[ ] Valida schema via Pydantic.
[ ] Trata erros de formato retornando um objeto de "falha segura" (Default: is_valid=False).
[ ] Coverage >85%
[ ] O serviço emite logs estruturados em formato JSON contendo, no mínimo: `timestamp`, `service`, `event`, `correlation_id`, `status`, `latency_ms` e `error_message` quando aplicável.

## Definition of Done

- [ ] Parser robusto implementado
- [ ] Suite de testes com strings "sujas" de exemplo
- [ ] PRD atualizado
